CDN版意思是不可由最终用户修改的已发布在CDN网络上的公共版本

实际使用逻辑是：

1.在页面中先引入CDN版，引入时可设置本地框架路径
先需要比较这两个框架的版本，具体怎样待定
在加载配置文件时会从本地框架中载人
在加载类组件时，需要比较这两个框架中包含的类的版本

2.在页面中先引入本地版，引入时可设置在CDN网络上的框架路径


实现基础条件：
框架支持跨域请求
CDN服务器上必须开放跨域请求
框架里都存放了所有类组件的文件目录清单，且记录上了版本号和更新日期
